# apps/ops — Internal Operations

A separate deployment on a separate hostname with separate access control, not a
route group inside the customer app. Sharing a process with the customer surface
is how an internal control leaks into a customer session.

Arrives in Stage 9.
